# Base URL of the running DoH server (the `serve`/`doh` mode in main.rs).
# Read client-side by lib/api.ts via axios, so it must be NEXT_PUBLIC_.
NEXT_PUBLIC_CLIENT_URL=http://127.0.0.1:8443
NEXT_PUBLIC_GA_ID="GA-xxx"
